# Production build config (inlined by Vite at `npm run build`).
# The built app is served by FastAPI on the same origin, so it calls the real
# API at a relative /api path — no mock, no separate API URL.
VITE_USE_MOCK=false
VITE_API_URL=
