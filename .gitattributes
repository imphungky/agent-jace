# Shell scripts in this repo are executed on the Linux droplet. Force LF so a
# Windows checkout (core.autocrlf=true) can't commit CRLF line endings, which
# would break the shebang with: `/usr/bin/env: 'bash\r': No such file or directory`.
*.sh text eol=lf
